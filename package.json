{
  "name": "escrow-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n contracts,web -c cyan,magenta \"npm run dev -w apps/contracts\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/web",
    "lint": "npm run lint -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
